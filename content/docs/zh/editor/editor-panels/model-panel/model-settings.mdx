---
title: 模型设置
description: 通过可视化操作手柄或参数面板设置模型的位置、旋转、缩放与可见性，实现精确的三维模型布局。
---

import { FeatureHero, VideoDemo } from '@/components/docs'

先拖动操作手柄完成大致摆放，再输入精确数值定位。单独使用其中一种方式都不足以完成工作。

操作手柄是选中模型后显示的彩色箭头与圆环。拖动手柄时模型随鼠标移动，速度快，但无法落在精确位置。在面板中输入数值可以精确定位，但操作较慢。

模型设置控制场景中每个模型的空间状态。


## 字段参考

| 字段 | 说明 | 推荐用法 |
| --- | --- | --- |
| 位置 | 模型在三维场景中的位置。 | 使用操作手柄快速摆放，使用参数数值精确对齐。 |
| 旋转 | 模型的朝向。 | 使模型与道路、房间、设备方向或其他场景参照对齐。 |
| 缩放 | 模型在场景中的尺寸。 | 保持相关资产之间缩放一致，使空间关系保持准确。 |
| 可见性 | 模型在当前场景中是否显示。 | 评审时隐藏辅助或临时资产，无需删除。 |

需先在大纲的模型图层中选中模型，其设置项才会显示。未选中任何模型时，面板会提示先选择模型。

## 设置模型位置

使用操作手柄或参数面板调整位置，使模型在场景中准确就位。正确的定位可保持资产之间的空间关系，使场景具备信息价值，而非仅堆放内容。

位置通常在模型加载完成后立即设置，并应在附加标记前确定：之后移动模型需重新调整其上的每个标记。

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-model-settings-1.mp4" poster="/demos/project-center/3.2.3模型面板-设置模型位置.poster.jpg" />

## 设置模型旋转

调整旋转，使模型朝向与场景语境一致。当导入资产的朝向不符合预期，或需与道路、房间、设备布局对齐时，需进行此项调整。

旋转偏差在地面网格的对照下最易发现，因为小角度旋转偏移在缺少参照时难以识别。

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-model-settings-2.mp4" poster="/demos/project-center/3.2.3模型面板-设置模型旋转.poster.jpg" />

## 设置模型缩放

调整缩放以匹配空间比例与展示要求。当多个资产共处同一场景，或项目需要传达真实空间关系时，一致性最为关键。

请结合地面网格与相机视角检查缩放。单独查看时正确的模型，在网格提供参照后可能显示比例失当；缩放误差在后期难以识别，因为长时间观看会降低对比例的敏感度。

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-model-settings-3.mp4" poster="/demos/project-center/3.2.3模型面板-设置模型缩放.poster.jpg" />

## 设置模型可见性

根据展示、调试或交互需求调整可见性。隐藏功能可聚焦于特定资产，而无需将其从项目中移除。

该功能支持分阶段演示、前后对比，以及在配置过程中临时清理辅助资产。此处设置的是编辑状态下的可见性；界面组件亦可在运行时控制模型显隐，详见[界面交互](/zh/docs/editor/editor-panels/ui-panel/add-interaction)。

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-model-settings-4.mp4" poster="/demos/project-center/3.2.3模型面板-设置模型可见性.poster.jpg" />
