---
title: 碰撞体与调试工具
description: 添加碰撞体，并确认标记的实际位置。
---

标记面板底部的两个区域。

## 碰撞体

列出场景中的碰撞体，并提供刷新按钮与用于添加的加号按钮。空状态显示「无碰撞体」。

当仅靠模型表面无法可靠拾取时，碰撞体可为标记提供附着对象。

## 调试工具

| 控件 | 作用 |
| --- | --- |
| 标记位置 | 显示所选标记在场景中的位置 |
| 碰撞体 | 绘制碰撞体，以便查看其形体 |

当标记在面板中显示正常，但在视口中位置错误时，请使用这两个控件。
