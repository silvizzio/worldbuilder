---
title: Debug Tools
description: Ground grid visualization and camera reset, two controls that make model positioning and scale review verifiable rather than subjective.
---
import { FeatureHero, VideoDemo } from '@/components/docs'

Turn on the ground grid before judging placement, and reset the camera before comparing iterations.
Both make placement disagreements checkable.

Placement problems are hard to settle without something to measure against.
One person says a model is floating above the ground, another says it looks fine, and neither can prove it.
It gets worse when the camera has been moved around, because every check happens from a different angle.


## Control reference

| Control | What it does | Use it when |
| --- | --- | --- |
| Show Ground Grid | Toggles a reference grid in the 3D viewport | Checking whether a model is floating, offset, or placed at an unexpected height |
| Reset Camera | Restores the view to the initial camera configured in the Scene Panel | The camera has moved far from the intended review angle |

## Show Ground Grid

Against an empty background these problems are nearly invisible.
Against evenly spaced grid lines they show up straight away:

- Floating models
- Vertical offsets
- Inconsistent scale
- Rotation errors, since an off-axis model is visibly misaligned relative to the grid lines

Most useful for object-level and space-level projects, where layout review needs to be reliable before preview or sharing.

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-debug-tools-1.mp4" poster="/demos/project-center/3.2.3模型面板-调试工具-地面网格.poster.jpg" />

## Reset Camera

Restores the current view to the initial camera configured in the [Scene Panel](/en/docs/editor/editor-panels/scene-panel/camera-controller).

Two benefits:

- Returns the view without manually navigating back after inspecting an asset up close
- Gives every check the same reference view, so comparisons between iterations happen from an identical angle

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-debug-tools-2.mp4" poster="/demos/project-center/3.2.3模型面板-调试工具-重置相机位置.poster.jpg" />
