---
title: Debug Tools
description: Show a ground grid and reset the camera, to check model placement.
---
import { ShotGrid, VideoDemo } from '@/components/docs'

Debug Tools provides a ground grid for checking model placement and a camera reset for returning to the same viewing angle. Together they help you spot changes in position, scale and alignment.

<ShotGrid images={['/demos/project-center/screens/debug-show-ground-grid.png', '/demos/project-center/screens/debug-reset-camera.png']} />

## Check whether a model sits correctly

1. Open the **Model Panel**.
2. Select **Show Ground Grid**.
3. Look at the model from the side.

The grid appears under the models.
A correctly placed model rests on the grid line.

The grid makes gaps, misalignment, and scale differences easier to spot.

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-debug-tools-1.mp4" poster="/demos/project-center/3.2.3模型面板-调试工具-地面网格.poster.jpg" />

## Return to the same view every time

1. Open the **Model Panel**.
2. Select **Reset Camera**.

The view returns to the opening angle set in the Scene Panel.

Use this before comparing two versions.
Otherwise each check happens from a different angle.

<VideoDemo src="/demos/project-center/web-videos/editor-editor-panels-model-panel-debug-tools-2.mp4" poster="/demos/project-center/3.2.3模型面板-调试工具-重置相机位置.poster.jpg" />

## What the grid reveals

| Problem | How it looks against the grid |
| --- | --- |
| Model floats | A gap between the model and the grid line |
| Model sinks | The base disappears below the grid |
| Wrong scale | The model spans too many or too few squares |
| Wrong rotation | Edges run at an angle to the grid lines |
