---
title: Product Overview
description: WORLD BUILDER turns reconstructed 3D assets into interactive digital twin applications that open in a browser.
---
import { ShotGrid } from '@/components/docs';

WORLD BUILDER turns a finished 3D reconstruction into an application a customer opens from a link.
No 3D engineering team, no engine licence, no release cycle.

It helps to know what you are starting from.
A **reconstruction** is the 3D model produced by scanning something real, such as a building, a site, or a product.
It is accurate to the centimetre, and on its own it is difficult to use.
Viewing it needs specialist software, editing it needs specialist skills,
and putting it in front of a customer needs a developer.

WORLD BUILDER sits on top of VIZZIO's reconstruction pipeline and removes those three requirements.
You bring a reconstruction in, arrange it, explain it, and publish it as a link.
Anyone you send that link to opens it in a normal browser, on a laptop or a phone,
with no software to install.

<ShotGrid images={['/demos/project-center/Overview.png']} />

## The problem this solves

| Without WORLD BUILDER | With WORLD BUILDER |
| --- | --- |
| A 3D file that needs specialist software to open | A URL that opens in any browser |
| A developer to build a viewer around it | A visual editor a non-engineer can operate |
| A release cycle to change anything | Edit, save, preview, reshare in minutes |
| Annotations and context in a separate document | Information attached to the position it describes |
| One asset, one use, rebuilt each time | A project structure that duplicates and adapts |

Treating the scan as a starting point rather than a finished file changes what happens next:

- **Before**: a one-off deliverable, finished once and rarely updated
- **After**: a foundation, where new versions start from a copy rather than a rebuild
  - Alternate versions for a review
  - Customer-specific variants
  - Updated captures of the same site

## Who this is for

WORLD BUILDER assumes a team. Each role owns an area and finishes where the next person starts.

| Role | What they do here | Where they work |
| --- | --- | --- |
| Project owner | Owns project identity, type, lifecycle, and access | Project Center, Project Settings |
| 3D content operator | Brings assets in, places them, checks scale and alignment | Model Panel, Debug Tools |
| Solution engineer | Adds business context and builds the interactive layer | Marker Panel, UI Panel |
| Reviewer or customer | Opens a link, explores, gives feedback | Preview and share links |
| Developer | Embeds the published Viewer into an existing product | WebUI Open API |

One consequence is worth stating plainly: the person who understands the business problem no longer has to be the person who can operate 3D software.
A solution engineer can add markers, build a guided walkthrough, and publish it without touching the reconstruction.

## Where it gets used

The platform stays the same. What changes is the asset level you start from.

- **Map-level**: planning, infrastructure, urban context
  - A reconstructed district or campus becomes a shared reference for planning review and stakeholder presentation
  - Survey data and asset condition sit on the location they describe, not in a report cross-referenced to a map
- **Space-level**: facilities, inspection, walkthroughs
  - A scanned building becomes something a facilities team, tenant, or remote contractor can explore without travelling
  - Video markers show how equipment operates, and image markers carry evidence the model cannot
- **Object-level**: products, equipment, training
  - Fly-to interactions move a viewer through each feature in sequence
  - Visibility toggles reveal internal components
  - Audio narration lets the scene run as a demonstration without anyone presenting it

Across all three, the outcome is the same: fewer site visits, fewer scheduled demonstrations, less context lost between capture and use.

## Platform capabilities

Four capabilities, each addressing a specific way 3D projects fail.

- **One pipeline, end to end**
  - VIZZIO handles capture, reconstruction, and asset generation as a single workflow
  - Assets arrive ready to use, skipping a conversion and cleanup pass that often outlasts the capture
- **Every asset level in one framework**
  - A campus, a building inside it, and equipment inside that are the same class of object
  - Teams at different scales share one set of conventions instead of three toolchains
- **Authoring in one place**
  - Scene configuration, asset placement, annotation, and interface design in the same editor
  - Replaces the usual split across a 3D tool, a document, and a developer
- **Publishing inside the workflow**
  - Preview, share, and access control belong to authoring, not a separate deployment
  - A project moves from edit to reviewable link without leaving the platform

## How the platform is structured

| Workspace | What it handles | Documentation |
| --- | --- | --- |
| Project Center | The portfolio layer: creating, finding, duplicating, sharing, and controlling access | [Project Center](/en/docs/project-center/) |
| Online Editor | The authoring layer: scene, models, markers, and UI across four feature panels | [Online Editor](/en/docs/editor/) |

The WebUI Open API sits outside both.
The API exposes scene, camera, model, marker, and interface control to an external application.
Use it when you want a published Viewer running inside a product you already own.

### Online Editor

Where a project becomes a scene. The 3D viewport, four feature panels, and publishing controls sit in one browser-based workspace.

The panel split is deliberate.
Scene configuration, model management, marker interaction, and UI design each own a layer, which is what keeps a complex project workable as it grows.

<ShotGrid images={['/demos/project-center/0.2在线编辑器.jpg']} />

[View Editor documentation](/en/docs/editor/)

### Project Center

Manages projects as objects rather than contents.
Each project carries its own identity, type, cover image, scene configuration, and publishing state.
That keeps a growing portfolio navigable when several scenarios run at once.

<ShotGrid images={['/demos/project-center/0.1项目管理.jpg']} />

[View Project Center documentation](/en/docs/project-center/)

## Asset levels

Asset level describes the scale of the 3D content. It is the first decision in any project, and it determines three things in sequence:

- Asset level sets the **project type**
- Project type sets the **default camera controller**
- The controller determines **how a viewer moves through the scene**

| Level | What it covers | Default controller | Typical outcome |
| --- | --- | --- | --- |
| Map | Scanned terrain, city blocks, campuses, outdoor sites | Third person, viewed from outside | Planning review, infrastructure assessment, spatial context |
| Space | Interiors, rooms, buildings, facilities | First person, walking through at eye level | Walkthroughs, remote inspection, tenant and contractor briefing |
| Object | Single objects, equipment, products, vehicles, parts | Third person, viewed from outside | Product demonstration, equipment training, component explanation |

The defaults follow from how someone should move around the scene.
You look at a map or an object from the outside, so the camera orbits around it like turning something over in your hands.
You go inside a room, so the camera walks through it at eye level.
You can change either one later.

## Where to start

| If you are | Start at |
| --- | --- |
| New to the platform | [Platform Concepts](/en/docs/platform-concepts/) |
| Building your first project | [End-to-End Workflow](/en/docs/end-to-end-workflow/) |
| Configuring an existing scene | [Feature Panels](/en/docs/editor/editor-panels/) |
| Preparing a project for a customer | [Publish Project](/en/docs/editor/project-actions/) |
| Integrating the Viewer into your own product | [WebUI Open API Reference](/en/docs/open-api/) |
